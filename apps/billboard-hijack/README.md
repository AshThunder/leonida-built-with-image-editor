# Billboard Hijack

Design rogue ads and composite them onto Leonida city plates. `#BuiltWithImageEditor`

```bash
npm install
npm run dev
npm run build
```

Hero path: Board strip → Hijack → Design studio (Image Editor) → Save → Composite / before-after → Gallery → Proof card. Takeovers in `localStorage`.
