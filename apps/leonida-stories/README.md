# Leonida Stories

Phone-first Stories toy for fictional Leonida NPCs. `#BuiltWithImageEditor`

```bash
npm install
npm run dev
npm run build
```

Hero path: Feed → Create → Pick/upload → Image Editor → Save → Preview → Post → Reactions → Share card PNG. Latest post in `localStorage`.
